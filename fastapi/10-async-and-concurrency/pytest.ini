[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
# auto mode means an async test function is collected without needing an
# @pytest.mark.asyncio decorator on every one of them.
asyncio_mode = auto
markers =
    timing: asserts wall-clock behaviour, so it is slower and less deterministic
