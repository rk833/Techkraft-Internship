[pytest]
# pythonpath puts this folder on sys.path so tests can `import main` without a
# package layout or a sys.path hack in conftest. Available in pytest 7+.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    contract: asserts the shape of the API contract rather than its behaviour
