# pinned to the versions this project was built and tested against.
# note these are LangChain 1.x, which differs from the 0.x API used by most
# older tutorials.
langchain==1.3.14
langchain-core==1.5.2
langchain-google-genai==4.3.2
python-dotenv==1.2.2

# module 04, draws chain structure as ascii via chain.get_graph().draw_ascii()
grandalf==0.8

# module 05, document loading and splitting.
# langchain-community is being sunset but still holds the only PDF loader with
# no standalone replacement. see 05-Document-Processing/README.md
langchain-community==0.4.2
langchain-text-splitters==1.1.2
pypdf==6.14.2
python-docx==1.2.0
tiktoken==0.13.0

# module 07, vector databases.
# langchain-chroma is the maintained standalone package. FAISS still comes from
# langchain-community. see 07-Vector-Databases/README.md
langchain-chroma==1.1.0
chromadb==1.5.9
faiss-cpu==1.14.3

# module 09, advanced retrievers.
# langchain-classic ships with langchain and holds the retrievers that were in
# langchain.retrievers before 1.x. lark is the query parser SelfQueryRetriever
# needs. see 09-Advanced-RAG/README.md
langchain-classic==1.0.8
lark==1.3.1

# module 10, retrieval optimisation.
# flashrank runs a cross encoder on onnxruntime, which chromadb already pulls
# in. sentence-transformers would do the same job but drags in torch.
# the reranker model itself is downloaded on first use and cached.
rank-bm25==0.2.2
FlashRank==0.2.10

# module 11, agents.
# no agent framework extras are needed. requests is listed explicitly because
# the tools call the Wikipedia and Open-Meteo APIs directly. the old wikipedia
# PyPI package is deliberately NOT used, see 11-Agents/README.md
requests>=2.32

# module 14, MCP.
# mcp MUST stay below 2.0. langchain-mcp-adapters 0.3.1 imports
# mcp.shared.context.RequestContext, which mcp 2.0 removed, so the adapter
# fails at import time against it. see 14-MCP/README.md
langchain-mcp-adapters==0.3.1
mcp==1.29.0
